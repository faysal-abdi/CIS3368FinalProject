<!DOCTYPE html>
<html>
<head>
    <title>Classrooms</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1>Classrooms</h1>
        <form method="POST" action="/classrooms">
            <div class="form-group">
                <label for="name">Name:</label>
                <input type="text" class="form-control" id="name" name="name" required>
            </div>

            <div class="form-group">
                <label for="capacity">Capacity:</label>
                <input type="number" class="form-control" id="capacity" name="capacity" required>
            </div>

            <div class="form-group">
                <label for="facility">Facility:</label>
                <input type="text" class="form-control" id="facility" name="facility" required>
            </div>

            <button type="submit" class="btn btn-primary">Add Classroom</button>
        </form>

        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Capacity</th>
                    <th>Facility</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% classrooms.forEach(function(classroom) { %>
                <tr>
                    <td><%= classroom.id %></td>
                    <td><%= classroom.name %></td>
                    <td><%= classroom.capacity %></td>
                    <td><%= classroom.facility %></td>
                    <td>
                       ```html
<form method="POST" action="/classrooms/<%= classroom.id %>?_method=PUT">
    <div class="form-group">
        <input type="text" class="form-control" name="name" value="<%= classroom.name %>" required>
    </div>
    <div class="form-group">
        <input type="number" class="form-control" name="capacity" value="<%= classroom.capacity %>" required>
    </div>
    <div class="form-group">
        <input type="text" class="form-control" name="facility" value="<%= classroom.facility %>" required>
    </div>
    <button type="submit" class="btn btn-sm btn-primary">Update</button>
</form>
<form method="POST" action="/classrooms/<%= classroom.id %>?_method=DELETE">
    <button type="submit" class="btn btn-sm btn-danger">Delete</button>
</form>
</td>
</tr>
<% }); %>
</tbody>
</table>
</div>
</body>
</html>